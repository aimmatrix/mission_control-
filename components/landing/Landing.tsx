// STUB — Agent 5 owns components/landing/** and replaces this file.
// Contract: default export, no props, renders the hero/marketing section.
export default function Landing() {
  return (
    <section className="py-10">
      <h1 className="text-3xl font-bold tracking-tight">
        Mission Control — every agent supervised.
      </h1>
      <p className="mt-3 text-ctrl-dim">
        Coding agents are fast. Unsupervised merges are how you lose a weekend
        of work.
      </p>
    </section>
  );
}
